One row per route inside the options sheet; exactly one is selected.

```jsx
<RouteOptionCard kind="recommended" tagLabel="Recomendada" distance="1.8 km"
  description="Ciclovias e vias calmas e residenciais" duration="8 min" selected />
```

Always explain *why* in `description` — never list a route without its rationale.
