import React from "react";

export function SectionLabel({ children, style, ...rest }) {
  return (
    <div
      style={{
        fontFamily: "var(--font-core)", fontSize: "var(--type-eyebrow-size)",
        fontWeight: "var(--type-eyebrow-weight)", letterSpacing: "var(--type-eyebrow-track)",
        textTransform: "uppercase", color: "var(--text-tertiary)", ...style,
      }}
      {...rest}
    >
      {children}
    </div>
  );
}
