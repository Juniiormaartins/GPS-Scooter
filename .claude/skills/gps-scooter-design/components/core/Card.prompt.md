The default surface: 20px radius, card fill, 1px hairline border, 16px padding.

```jsx
<Card tone="card">…</Card>
```

Use `tone="overlay"` + `blur` only when the card floats above the map. No drop shadow on in-flow cards — elevation reads as a lighter fill.
