Icon-only control for headers, search fields and map overlays.

```jsx
<IconButton icon="x" label="Encerrar navegação" tone="neutral" />
```

`tone="overlay"` for controls floating on the map; `quiet` inside a card or nav bar. Never smaller than 44px.
