The explore screen's header: avatar, origin line, destination line, search icon — blurred overlay over the map.

```jsx
<RouteSearchField origin="Sua localização" destination="Para onde?" onPress={openSearch} />
```
