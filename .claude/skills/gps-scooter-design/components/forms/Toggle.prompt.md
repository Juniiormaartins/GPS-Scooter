On/off preference control. On = green; off = slate track with a muted knob.

```jsx
<Toggle checked={avoidHighways} onChange={setAvoidHighways} label="Evitar vias rápidas" />
```

Use inside `SettingsRow` rather than standalone.
