Renders a single Lucide glyph — the icon primitive every other component uses.

```jsx
<Icon name="map-pin" size={24} color="var(--accent-primary)" />
```

Lucide is loaded lazily from CDN on first use. Icons are always monoline (stroke 2, round caps); never fill them and never mix in emoji.
