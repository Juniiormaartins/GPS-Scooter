Trip summary metrics — distance, duration, average speed, CO₂ saved.

```jsx
<StatTile label="Economia de CO₂" value="240g" tone="go" />
```

Always in a `grid-template-columns:1fr 1fr` with 12px gap. Reserve `tone="go"` for the one number worth celebrating.
